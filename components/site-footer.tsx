"use client";
import Link from "next/link";
import { company, nav, pick, services } from "@/lib/content";
import { useLanguage } from "./language-provider";

export function SiteFooter(){const{lang}=useLanguage();return <footer className="site-footer"><div className="footer-main"><div className="footer-intro"><Link className="footer-brand" href="/"><span className="logo-crop"><img src="/rsc-emblem-only-v5.webp" alt=""/></span><span><strong>{pick(company.name,lang)}</strong><small>{lang==="ar"?"للمقاولات العامة":"GENERAL CONTRACTING"}</small></span></Link><p>{lang==="ar"?"حلول مقاولات عامة تنطلق من وضوح النطاق، وتُبنى على جودة التنفيذ، وتكتمل بتسليم مسؤول.":"General contracting solutions built on clear scope, controlled execution and accountable handover."}</p></div><div><h3>{lang==="ar"?"الشركة":"Company"}</h3>{nav.slice(1).map(x=><Link href={x.href} key={x.href}>{pick(x.label,lang)}</Link>)}</div><div><h3>{lang==="ar"?"الخدمات":"Services"}</h3>{services.slice(0,5).map(x=><Link href={`/services/${x.slug}`} key={x.slug}>{pick(x.title,lang)}</Link>)}</div><div><h3>{lang==="ar"?"التواصل":"Contact"}</h3><a dir="ltr" href={`tel:${company.phoneRaw}`}>{company.phone}</a><span>{pick(company.location,lang)}</span><a href={`https://wa.me/${company.phoneRaw}`} target="_blank" rel="noopener noreferrer">WhatsApp</a></div></div><div className="footer-bottom"><span>© {new Date().getFullYear()} {pick(company.legal,lang)}</span><div><Link href="/privacy">{lang==="ar"?"سياسة الخصوصية":"Privacy"}</Link><Link href="/terms">{lang==="ar"?"الشروط والأحكام":"Terms"}</Link></div></div></footer>}
